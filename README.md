# Project 1 — Data Science (Regression & Model Comparison)

**Author:** Venkata Vivek Panguluri

## Overview
This project explores regression modeling (e.g., Linear, Ridge, Lasso) and compares performance using RMSE/R².

## Repo Structure
- `notebooks/` – Jupyter notebooks for EDA/modeling  
- `src/` – Reusable Python modules (preprocessing, models, metrics)  
- `results/` – Saved figures/metrics  
- `docs/` – Report (PDF)

## Getting Started
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
jupyter lab   # or: jupyter notebook
cat > README.md << 'EOF'

